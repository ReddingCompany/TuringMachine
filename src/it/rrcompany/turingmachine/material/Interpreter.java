package it.rrcompany.turingmachine.material;

public interface Interpreter {

    char
        NOT = '^',
        RIGHT = '>',
        LEFT = '<',
        NULL = '-';

    String
        BETWEEN = "..";

}
